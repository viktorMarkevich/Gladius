%h1 Listing users

.well
  %table.table.table-striped.pretty
    %tr
      %th ID
      %th ФИО
      %th Status

    - @users.each do |user|
      %tr
        %td= user.id
        %td= link_to_if( user == current_user, user.full_name, user_path(current_user))
        %td= user.status
%br

= link_to 'New User', new_user_path
