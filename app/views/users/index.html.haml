%h1 Listing users

.well
  %table.table.table-striped.pretty
    %tr
      %th ID
      %th Photo
      %th ФИО
      %th Status
      %th Role
      %th Возможные операции

    - @users.each do |user|
      %tr
        %td= user.id
        %td= image_tag user.avatar.url(:large)
        %td= link_to user.full_name, user_path(user)
        %td= user.status
        %td= user.role
        - if user.school_id == current_user.school_id
          %td= link_to_if((can? :update, user) || (user == current_user) , "Редактировать", edit_user_path(user))
        - else
          %td= link_to_if((can? :update, user) || (user == current_user) , "Добавить в школу", edit_user_path(user))
