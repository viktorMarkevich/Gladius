%h1 Editing user

= form_for(@user_school_relation, :html => {:multipart => true}, :url => school_user_path, :method => :put) do |f|
  = render :partial => 'form', :locals => { :f => f }

= link_to 'Show', @user
\|
= link_to 'Back', users_path
