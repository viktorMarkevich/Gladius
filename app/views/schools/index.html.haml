%h1 Listing schools

.well
  %table.table.table-striped.pretty
    %tr
      %th ID
      %th Название школы
      %th Количество учеников
      %th Редактировать школу
      %th Удалить школу

    - @schools.each do |school|
      %tr
        %td= school.id
        %td= link_to school.school_name, school
        %td= link_to school.user_school_relations.count, school_members_path(school)
        %td= link_to "Edit", [:edit, school]
        %td= link_to "Delete", school_path(school), :method => :DELETE