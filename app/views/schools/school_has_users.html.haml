%h1 Список учеников

.well
  %table.table.table-striped.pretty
    %tr
      %th ID
      %th Photo
      %th ФИО
      %th Status
      %th Role
      %th Редактировать профиль
      %th Отчислить

    - @users_of_school.each do |user|
      %tr
        %td= user.id
        %td= image_tag user.avatar.url(:large)
        %td= link_to user.full_name, user_path(user)
        %td= user.status
        %td= user.role
        %td= link_to_if((can? :update, user) || (user == current_user), "Редактировать", edit_user_path(user))
        %td= link_to_if((can? :destroy, user), "Отчислить ученика", school_expelled_path(:school_id => @school.id, :id => user.id))

= link_to 'Добавить Нового участника школы', new_user_path if (can? :manage, @school)
