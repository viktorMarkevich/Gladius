%h3 Listing members
- if user_signed_in?
  %p
    = link_to 'New member', new_school_member_path(@school)
.well
  %table.table.table-striped.pretty
    %tr
      %th ID
      %th Photo
      %th FSL
      %th Status
      %th Actions

    - @members.each do |member|
      %tr
        %td= member.id
        %td= image_tag member.image.avatar.url(:large)
        %td= link_to member.full_name, school_member_path(:id => member.id)
        %td= member.user_school_relation.status
        %td= link_to 'Deduct', school_member_path(:id => member.id), :method => :delete
